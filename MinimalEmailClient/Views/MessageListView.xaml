<UserControl x:Class="MinimalEmailClient.Views.MessageListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:views="clr-namespace:MinimalEmailClient.Views"
             xmlns:viewModels="clr-namespace:MinimalEmailClient.ViewModels"
             xmlns:prism="http://prismlibrary.com/"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600">

    <i:Interaction.Triggers>
        <!-- Popup for opening the selected message -->
        <prism:InteractionRequestTrigger SourceObject="{Binding OpenSelectedMessagePopupRequest, Mode=OneWay}">
            <prism:PopupWindowAction>
                <prism:PopupWindowAction.WindowContent>
                    <views:SelectedMessageView />
                </prism:PopupWindowAction.WindowContent>
            </prism:PopupWindowAction>
        </prism:InteractionRequestTrigger>
    </i:Interaction.Triggers>

    <UserControl.DataContext>
        <viewModels:MessageListViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <!-- Converts Message.IsSeen to envelope image source string. -->
        <views:BooleanToEnvelopImageSourceConverter x:Key="BooleanToEnvelopImageSourceConverter" />

        <!-- Style for TextBlock in ListView column cell -->
        <Style x:Key="CellTextBlockStyle" TargetType="TextBlock">
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSeen}" Value="False">
                    <Setter Property="FontWeight" Value="Bold" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>

        <!-- The listview -->
        <ListView x:Name="messageListView" BorderThickness="0" ItemsSource="{Binding Messages}" SelectionMode="Extended"  SelectedItem="{Binding SelectedMessage}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" AlternationCount="2">

            <ListView.Background>
                <ImageBrush ImageSource="/Resources/Images/watermark1.png" Stretch="None" Opacity="0.5" />
            </ListView.Background>

            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <i:InvokeCommandAction Command="{Binding OpenSelectedMessageCommand}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>

            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Delete" Command="{Binding DeleteMessageCommand}" />
                </ContextMenu>
            </ListView.ContextMenu>

            <ListView.View>
                <GridView>
                    <!-- 'IsSeen' column -->
                    <views:FixedWidthGridViewColumn x:Name="col0" FixedWidth="30">
                        <views:FixedWidthGridViewColumn.Header>
                            <Image Source="/Resources/Images/envelope.png" Height="14" />
                        </views:FixedWidthGridViewColumn.Header>
                        <views:FixedWidthGridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding IsSeen, Converter={StaticResource BooleanToEnvelopImageSourceConverter}}" Height="14" Margin="2, 0, -2, 0" />
                            </DataTemplate>
                        </views:FixedWidthGridViewColumn.CellTemplate>
                    </views:FixedWidthGridViewColumn>

                    <!-- 'From' column -->
                    <GridViewColumn x:Name="col1" Header="From" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding SenderAddress}" Style="{StaticResource CellTextBlockStyle}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- 'Subject' column -->
                    <GridViewColumn Header="Subject" Width="{Binding ElementName=helperField, Path=ActualWidth}">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Subject}" Style="{StaticResource CellTextBlockStyle}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- 'Date' column -->
                    <GridViewColumn x:Name="col3" Header="Date" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Date}" Style="{StaticResource CellTextBlockStyle}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- This is the helper to resize the above ListView columns. -->
        <Grid Visibility="Hidden">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{Binding ElementName=col0, Path=ActualWidth}"/>
                <ColumnDefinition Width="{Binding ElementName=col1, Path=ActualWidth}"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="{Binding ElementName=col3, Path=ActualWidth}"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="2" Name="helperField"/>
        </Grid>

    </Grid>
</UserControl>
